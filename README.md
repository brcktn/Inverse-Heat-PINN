# Inverse-Heat-PINN
Project for CS474 (Deep Learning) at BYU. Uses a physics informed neural network (PINN) to determine the temperature distribution over time for a flat plate based on temperature data over time rom sparse input points. Also includes a script to generate test inputs using finite difference methods.
